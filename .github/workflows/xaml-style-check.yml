name: XAML Style Check

on:
  pull_request:
    paths:
      - 'src/**/*.xaml'
      - 'Settings.XamlStyler'
      - '.config/dotnet-tools.json'
      - '.github/workflows/xaml-style-check.yml'
  workflow_dispatch:

env:
  DOTNET_NOLOGO: 1
  DOTNET_CLI_TELEMETRY_OPTOUT: 1

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  xaml-style:
    name: XAML Style Check
    # Actions minutes are free on public repos but billed on private/internal
    # ones, so only run this check when the repository is public.
    if: github.event.repository.private == false
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      # No setup-dotnet: the runner image's preinstalled .NET 10 SDK suffices
      # for tool restore + XAML Styler.
      - name: Restore dotnet tools
        run: dotnet tool restore

      - name: Run XAML Styler
        run: dotnet xstyler -c Settings.XamlStyler -r -d ./src

      - name: Detect drift
        id: drift
        shell: bash
        run: |
          if [ -z "$(git status --porcelain)" ]; then
            echo "drift=false" >> "$GITHUB_OUTPUT"
            echo "No drift detected."
          else
            echo "drift=true" >> "$GITHUB_OUTPUT"
            echo "::warning::XAML Styler drift detected."
            git add -A
            git diff --cached > xaml-style.patch
            echo "---- git status ----"
            git status
            echo "---- git diff (truncated to 400 lines) ----"
            git -c color.ui=always diff --cached | head -n 400 || true
          fi

      - name: Upload patch artifact
        if: steps.drift.outputs.drift == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: xaml-style-patch
          path: xaml-style.patch
          if-no-files-found: error

      - name: Post drift comment
        if: steps.drift.outputs.drift == 'true' && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        env:
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        with:
          script: |
            const shortSha = process.env.HEAD_SHA.slice(0, 7);
            const body = [
              `### XAML Styler drift detected on \`${shortSha}\``,
              '',
              'XAML files under `src/` are not formatted according to `Settings.XamlStyler` (at the repository root).',
              '',
              '**To apply automatically** (PRs from branches in this repo only): comment `/apply-xaml-style` on this PR and a bot will push `chore: Apply XAML styler` to the head branch.',
              '',
              '**To apply manually** (required for PRs from forks):',
              `1. Download the \`xaml-style-patch\` artifact from [this run](${process.env.RUN_URL}).`,
              '2. In your local checkout of the PR branch:',
              '   ```',
              '   git apply xaml-style.patch',
              '   git add -A',
              '   git commit -m "chore: Apply XAML styler"',
              '   git push',
              '   ```',
              '',
              'Or re-run the formatter locally: `dotnet tool restore && dotnet xstyler -c Settings.XamlStyler -r -d ./src`.',
            ].join('\n');
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body,
            });

      - name: Fail if drift detected
        if: steps.drift.outputs.drift == 'true'
        shell: bash
        run: exit 1
