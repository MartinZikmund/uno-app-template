<ContentDialog
    x:Class="AppTemplate.Dialogs.ColorPickerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppTemplate.Dialogs"
    xmlns:markup="using:AppTemplate.Markup"
    Title="{markup:Localize Key=ColorPickerTitle}"
    PrimaryButtonText="{markup:Localize Key=Select}"
    CloseButtonText="{markup:Localize Key=Cancel}"
    DefaultButton="Primary">

    <!-- Content -->
    <ColorPicker
        x:Name="ColorPickerControl"
        ColorSpectrumShape="Box"
        IsAlphaEnabled="True"
        IsColorChannelTextInputVisible="True"
        IsColorPreviewVisible="True"
        IsHexInputVisible="True" />

</ContentDialog>
