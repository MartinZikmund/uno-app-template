<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Scheduled notification permissions - see the "Android: scheduled notifications & boot
       rescheduling" section in README.md. SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM are special-access
       permissions with Play Store policy implications; drop them (the code already falls back to an
       inexact alarm) unless the app genuinely needs exact-time delivery. -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
  <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
  <application android:allowBackup="true" android:supportsRtl="true"></application>
</manifest>
